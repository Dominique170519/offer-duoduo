# 复制本文件为 .env 并填入真实值。
# .env 已被 .gitignore 忽略，不会提交到仓库；不要在这里填写真实密钥后提交。
# 环境变量优先级高于 .env：如果 shell 已 export 同名变量，以 shell 为准。

# DeepSeek（或兼容 OpenAI 格式的）API Key
AI_API_KEY=sk-your-key-here

# 可选：模型服务地址与模型名（默认已指向 DeepSeek，通常无需修改）
# AI_BASE_URL=https://api.deepseek.com
# AI_MODEL=deepseek-chat
